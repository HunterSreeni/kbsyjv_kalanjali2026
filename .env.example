VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# WARNING: use a REAL Cloudflare Turnstile site key here for production, never the
# test key (1x00000000000000000000AA) - the test key always passes and disables
# bot-checking entirely. Set the matching TURNSTILE_SECRET_KEY as a Supabase Edge
# Function secret (`supabase secrets set TURNSTILE_SECRET_KEY=...`), not here.
VITE_TURNSTILE_SITE_KEY=
